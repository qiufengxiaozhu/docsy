version: '3.8'

services:
  docsy:
    build: .
    container_name: docsy
    ports:
      - "8080:8080"
    volumes:
      - docsy-data:/app/data
    environment:
      - DOCSY_JWT_SECRET=your-production-jwt-secret-change-this
      - TZ=Asia/Shanghai
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  docsy-data:
    driver: local
