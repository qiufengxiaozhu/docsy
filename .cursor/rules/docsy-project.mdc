---
description: Docsy 项目架构、技术选型、开发规范和注意事项
alwaysApply: true
---

# Docsy 项目概览

文档预览与编辑服务，三方业务系统通过 API 获取可嵌入的 iframe URL，实现 Office、PDF、图片、音视频文件的在线预览和编辑。

## 技术栈

- **后端**: Java 21 + Spring Boot 3.4.4 + SQLite (sqlite-jdbc) + Spring Data JDBC
- **前端**: Vue 3 + Vite + Element Plus（管理后台）
- **Office/PDF 引擎**: OnlyofficePersonal 9.3（纯 WASM，浏览器内运行，无服务端）
- **预览页面模板**: Thymeleaf
- **构建工具**: Maven
- **容器化**: Docker 多阶段构建（Node + Maven + JRE 21）
- **认证**: JWT（管理后台） + HMAC-MD5 签名（三方应用，与 docsz 一致）

## 项目结构

```
docsy/
├── backend/                  # Spring Boot 后端（Maven 子模块）
│   ├── pom.xml
│   └── src/main/java/com/docsy/
│       ├── config/           # 配置（DocsyProperties, ViewerRegistry, WebConfig）
│       ├── controller/       # REST API + Thymeleaf 页面控制器
│       ├── model/            # entity/ + dto/
│       ├── repository/       # Spring Data JDBC 接口
│       ├── service/          # 业务逻辑
│       └── security/         # JWT + HMAC 认证
├── frontend/                 # Vue 3 管理后台
│   ├── src/views/            # dashboard, apps, files, sessions, fonts, settings, login
│   ├── src/components/       # Layout.vue 侧边栏布局
│   └── src/stores/           # Pinia (user store)
├── onlyoffice/               # OnlyOffice WASM 静态资源（运行时安装，不在 git 中）
├── scripts/setup-onlyoffice.sh
├── Dockerfile
└── docker-compose.yml
```

## 核心架构设计

### ViewerRegistry（格式可扩展）

`ViewerRegistry.java` 维护文件格式到渲染器类型的映射：
- ONLYOFFICE: docx/doc/xlsx/xls/pptx/ppt/pdf/odt/ods/odp/rtf/epub
- IMAGE: png/jpg/gif/bmp/svg/webp
- MEDIA: mp4/webm/mp3/wav/ogg/flac
- CODE: md/txt/xml/html/json/yaml/java/py/js/ts/css/sql/sh
- UNSUPPORTED: 其他格式（提供下载）

新增格式只需在 ViewerRegistry 构造函数中添加 registerAll 调用。

### 预览 UI Shell

所有 iframe 预览页面共享统一 UI 框架（viewer-shell.html Fragment）：
- 深色工具栏（对齐 OnlyOffice 风格）+ 文件名/模式标记 + 下载/全屏按钮
- 浅色内容区（各渲染器填充）
- 底部状态栏（文件信息）

### OnlyOffice WASM 通信协议

```javascript
// 打开文件
iframe.contentWindow.postMessage({
  type: 'document:open-url',
  payload: { url, fileName, fileType, mode }
}, '*');

// 监听保存（用户编辑后，WASM 返回文件 Blob）
window.addEventListener('message', (e) => {
  if (e.data?.type === 'document:saved') {
    const fileBlob = e.data.payload.file;
    fetch('/api/files/' + sessionId + '/content', { method: 'PUT', body: fileBlob });
  }
});
```

### 三方应用集成

签名算法与 docsz 一致：
- Headers: `X-App-Id`, `X-Timestamp`, `X-Nonce`, `X-Sign`
- Sign: `MD5(app_secret + "@@" + timestamp + "@@" + nonce)`
- 有效期: 300秒（configurable）

核心 API 流程:
1. `POST /api/preview/create` — 上传文件或提供 URL → 返回 sessionId + previewUrl
2. 三方系统 iframe 嵌入 `previewUrl`
3. 用户编辑保存后，后端回调三方 `callbackUrl`

## 开发注意事项

1. **SQLite 限制**: 不支持并发写入，适合中小规模部署。如需高并发可切换为 MySQL。
2. **OnlyOffice WASM 资源**: 不纳入 git，需运行 `scripts/setup-onlyoffice.sh` 安装。
3. **前端路由**: 管理后台路径为 `/admin/`，预览页面路径为 `/view/{sessionId}`。
4. **CORS**: 管理后台前端开发时通过 Vite proxy 代理 `/api` 到 8080。
5. **密码**: 默认管理员 admin/admin123（BCrypt 加密）。
6. **参考项目**: 旧版 docsy 在 `/opt/docsy`（WSL），前端使用 Vben Admin，可参考其 UI 风格。
7. **连接器 (Connector)**: OnlyOffice 的 Automation API，可通过 `docEditor.createConnector()` 在父页面操控编辑器内部文档内容（高级功能，非必须）。
8. **文件流**: 编辑后 WASM 通过 postMessage 返回完整文件 Blob 给父页面上传后端。
9. **Spring Data JDBC**: 使用 CrudRepository 接口，实体类用 `@Table` 和 `@Id` 注解。
10. **Thymeleaf Fragment**: 预览页面使用 `th:replace` 引用公共 Shell。

## 与 docsz 的关系

- docsz（`/opt/docsz`）：文档格式转换服务（Python + WPS Office）
- docsy：文档预览与编辑服务（Java + OnlyOffice WASM）
- 共用相同的 HMAC-MD5 三方签名认证机制
- docsy 前端参考 docsz 的 Element Plus 风格

## 数据库 Schema 关键表

- `sys_user` — 管理员（username, password/BCrypt）
- `app` — 三方应用（app_id, app_secret, is_active, rate_limit）
- `preview_session` — 预览/编辑会话（session_id, file_path, viewer_type, mode, callback_url, expires_at）
- `font` — 字体（font_name, file_path, source）
- `sys_config` — 系统配置 KV

## GitHub

- 仓库: https://github.com/qiufengxiaozhu/docsy
- 分支: main
- License: MIT
